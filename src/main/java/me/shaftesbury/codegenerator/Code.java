package me.shaftesbury.codegenerator;

public class Code {
    public Exception execute() {
        return null;
    }
    // Code is made up from an ordered sequence of ExecutableStatements, which
    // come from tokens. Parse the tokens to get the meaning and expect to be able
    // to execute them in the full context (after code generation)
}
